    @using Microsoft.AspNetCore.Identity
    @using OSMD.Models
    @*@model IEnumerable<OSMD.Models.Person>*@
    @inject SignInManager<ApplicationUser> SignInManager
    @inject UserManager<ApplicationUser> UserManager

    @if (SignInManager.IsSignedIn(User))
    { @*byte[] jak=null;
        @foreach (var b in Model)
        {
            jak = b.Avatar;
        }*@
        <form asp-area="" asp-controller="Account" asp-action="Logout" method="post" id="logoutForm" class="navbar-right">
            <ul class="nav navbar-nav navbar-right">
                @*<li>
                    <a asp-controller="Home" asp-action="Create_Avatar">Add avatar</a>
                </li>*@

                
                @*<li>
                    
                        @if (jak != null)
                        {
                            <img style='width:80px; height:60px;' src="data:image/jpeg;base64,@(Convert.ToBase64String(jak))" />
                        }
                        else
                        {
                            <img style='width:80px; height:60px;' src="~/images/dnepr.svg" />

                        }
                </li>*@
                <li>
                    <a asp-area="" asp-controller="Manage" asp-action="Index" title="Manage">Hello @UserManager.GetUserName(User)!</a>
                </li>
                <li>
                    <button type="submit" class="btn btn-link navbar-btn navbar-link">Log out</button>
                </li>
            </ul>
        </form>
    }
    else
    {

        <ul class="nav navbar-nav navbar-right">
            <li><img style='width:80px; height:60px;' src="~/images/dnepr.svg" /></li>
            <li><a asp-area="" asp-controller="Account" asp-action="Register">Registration</a></li>
            <li><a asp-area="" asp-controller="Account" asp-action="Login">Login</a></li>
        </ul>

    }
